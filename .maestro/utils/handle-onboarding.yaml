appId: com.bearstudio.startuinative.development
---
# Two screens in the onboarding — each button tap is conditional on the text being visible
- runFlow:
    when:
      visible: "Continue"
    commands:
      - tapOn: "Continue"
- runFlow:
    when:
      visible: "Continuer"
    commands:
      - tapOn: "Continuer"
- runFlow:
    when:
      visible: "Let's start"
    commands:
      - tapOn: "Let's start"
- runFlow:
    when:
      visible: "Commencer"
    commands:
      - tapOn: "Commencer"
